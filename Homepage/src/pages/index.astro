---
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="SuigenerisBD | Uniquely Engineered">
  <div class="wrapper">
    <div class="paper-surface">
      <!-- Margin Rail -->
      <aside class="margin-rail">
        <div class="vertical-text">
          EST. 2026 — DHAKA
        </div>
      </aside>

      <!-- Main Content -->
      <main class="main-content">
        <!-- Background Pattern -->
        <svg class="bg-pattern" viewBox="0 0 200 200" xmlns="http://www.w3.org/2000/svg">
          <path fill="var(--ink-primary)" d="M45.7,-76.3C58.9,-69.3,69.1,-58.3,77.3,-46.3C85.5,-34.3,91.7,-21.3,90.4,-8.9C89.1,3.5,80.3,15.3,71.7,26.4C63.1,37.5,54.7,47.9,44.6,56.3C34.5,64.7,22.7,71.1,10.2,72.6C-2.3,74.1,-15.5,70.7,-27.6,65.3C-39.7,59.9,-50.7,52.5,-60.1,43.2C-69.5,33.9,-77.3,22.7,-79.8,10.4C-82.3,-1.9,-79.5,-15.3,-72.6,-26.8C-65.7,-38.3,-54.7,-47.9,-43.1,-55.5C-31.5,-63.1,-19.3,-68.7,-6.6,-67.7C6.1,-66.7,18.3,-59.1,32.5,-83.3"></path>
        </svg>

        <!-- Header -->
        <Header currentPath="/" />

        <!-- Hero Section -->
        <section class="hero animate-up delay-2">
          <div class="calligraphy-container draw-on-scroll">
            <svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
              <path class="stroke" d="M20,30 Q40,10 60,30 T80,30 M30,50 C30,30 70,30 70,50 S30,70 30,50 M50,20 L50,80"></path>
              <path class="stroke" d="M15,60 Q30,85 50,60 T85,60" style="transition-delay: 0.5s;"></path>
            </svg>
          </div>

          <span class="hero-overline">From Dhaka to Digital</span>
          <h1>Uniquely<br />Engineered.</h1>
          <p>We craft bespoke software solutions rooted in precision and built for the global stage. Minimal clutter, maximum impact.</p>
          <a href="/contact" class="btn-ink">Start Project_</a>
        </section>

        <!-- Core Capabilities Section -->
        <section class="grid-section animate-up delay-3">
          <div class="grid-col-12">
            <h2 style="font-size: 1.2rem; margin-bottom: 40px; border-bottom: 1px solid rgba(0,0,0,0.1); padding-bottom: 10px;">CORE CAPABILITIES</h2>
          </div>

          <div class="service-card grid-col-4">
            <h3>01.</h3>
            <h4>System Architecture</h4>
            <p style="margin-top:12px;">Building resilient backends that scale with your vision. Robust, secure, and uniquely structured.</p>
          </div>

          <div class="service-card grid-col-4">
            <h3>02.</h3>
            <h4>Native Interface</h4>
            <p style="margin-top:12px;">UI/UX design that respects cultural nuance while adhering to modern usability standards.</p>
          </div>

          <div class="service-card grid-col-4">
            <h3>03.</h3>
            <h4>Digital Sovereignty</h4>
            <p style="margin-top:12px;">SEO-optimized platforms ensuring your voice is heard clearly in the crowded digital space.</p>
          </div>
        </section>

        <!-- Fusion Section -->
        <section class="grid-section animate-up delay-3" style="align-items: center; margin-bottom: 60px; position: relative;">
          <div class="calligraphy-container draw-on-scroll" style="left: -100px; right: auto; width: 300px;">
            <svg viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
              <path class="stroke" d="M10,50 Q30,10 50,50 T90,50 M20,20 L80,80 M80,20 L20,80"></path>
              <circle class="stroke" cx="50" cy="50" r="40" style="transition-delay: 0.8s;"></circle>
            </svg>
          </div>

          <div class="grid-col-6">
            <h2 style="font-size: 3rem;">A fusion of art &amp; logic.</h2>
          </div>
          <div class="grid-col-6">
            <p style="margin-bottom: 24px;">Just as the intricate strokes of Bengali calligraphy balance form and meaning, our code balances aesthetics and performance.</p>
            <p>We strip away the non-essential to reveal the core purpose of your digital product.</p>
          </div>
        </section>

        <!-- Footer -->
        <Footer />
      </main>
    </div>
  </div>
</Layout>

<style is:global>
  @import url('https://fonts.googleapis.com/css2?family=Rozha+One&family=Space+Mono:ital,wght@0,400;0,700;1,400&display=swap');

  :root {
    --bg-void: #1a1a1a;
    --bg-paper: #0a0a0a;
    --ink-primary: #F0F0F0;
    --ink-secondary: #A0A0A0;
    --accent-margin: #E69138;
    --font-display: 'Rozha One', serif; 
    --font-tech: 'Space Mono', monospace; 
    
    --spacing-unit: 8px;
    --container-width: 1200px;
    --transition-speed: 0.4s;
  }

  [data-theme="light"] {
    --bg-void: #CD202C;
    --bg-paper: #FDFBF7;
    --ink-primary: #0F0F0F;
    --ink-secondary: #4A4A4A;
    --accent-margin: #CD202C; 
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }
  
  body {
    background-color: var(--bg-void);
    color: var(--ink-primary);
    font-family: var(--font-tech);
    line-height: 1.6;
    overflow-x: hidden;
    transition: background-color var(--transition-speed) ease;
    -webkit-font-smoothing: antialiased;
  }

  h1, h2, h3 {
    font-family: var(--font-display);
    font-weight: 400; 
    line-height: 1.1;
  }

  .wrapper {
    display: flex;
    justify-content: center;
    padding: 4vh 2vw;
    min-height: 100vh;
  }

  .paper-surface {
    background-color: var(--bg-paper);
    width: 100%;
    max-width: var(--container-width);
    min-height: 92vh;
    position: relative;
    box-shadow: 0 20px 50px rgba(0,0,0,0.2);
    display: grid;
    grid-template-columns: 80px 1fr; 
    transition: background-color var(--transition-speed) ease, color var(--transition-speed) ease;
  }

  .margin-rail {
    height: 100%;
    border-right: 1px solid rgba(0,0,0,0.05);
    position: relative;
    display: flex;
    flex-direction: column;
    align-items: center;
    padding-top: 40px;
  }

  .vertical-text {
    writing-mode: vertical-rl;
    transform: rotate(180deg);
    margin-top: auto;
    padding-bottom: 80px;
    padding-top: 20px;
    color: var(--accent-margin);
    font-size: 0.7rem;
    letter-spacing: 2px;
    text-transform: uppercase;
  }

  .margin-rail::after {
    content: '';
    position: absolute;
    right: 12px; 
    top: 0;
    bottom: 0;
    width: 4px;
    border-left: 1px solid var(--accent-margin);
    border-right: 1px solid var(--accent-margin);
    opacity: 0.8;
  }

  .main-content {
    padding: 60px 80px;
    position: relative;
  }



  .hero {
    margin-bottom: 120px;
    position: relative;
  }

  .hero-overline {
    color: var(--bg-void);
    font-size: 0.9rem;
    font-weight: 700;
    margin-bottom: 16px;
    display: block;
    text-transform: uppercase;
    letter-spacing: 2px;
  }
  [data-theme="dark"] .hero-overline { color: var(--accent-margin); }

  .hero h1 {
    font-size: clamp(3rem, 8vw, 6.5rem);
    margin-bottom: 32px;
    color: var(--ink-primary);
  }

  .hero p {
    max-width: 500px;
    color: var(--ink-secondary);
    font-size: 1.1rem;
    margin-bottom: 48px;
  }

  .btn-ink {
    display: inline-flex;
    align-items: center;
    padding: 16px 32px;
    background: var(--ink-primary);
    color: var(--bg-paper);
    text-decoration: none;
    font-weight: 700;
    font-size: 0.9rem;
    border: 1px solid var(--ink-primary);
    transition: all 0.3s ease;
    clip-path: polygon(0 0, 100% 0, 100% 85%, 95% 100%, 0 100%); 
  }

  .btn-ink:hover {
    background: transparent;
    color: var(--ink-primary);
    transform: translateY(-2px);
    box-shadow: 4px 4px 0 var(--bg-void);
  }

  .grid-section {
    display: grid;
    grid-template-columns: repeat(12, 1fr);
    gap: 24px;
    margin-bottom: 120px;
  }

  .grid-col-4 { grid-column: span 4; }
  .grid-col-6 { grid-column: span 6; }
  .grid-col-12 { grid-column: span 12; }

  .service-card {
    border-top: 2px solid rgba(0,0,0,0.1);
    padding-top: 24px;
    transition: 0.3s;
  }
  
  .service-card:hover {
    border-color: var(--bg-void);
  }

  .service-card h3 {
    font-size: 2rem;
    margin-bottom: 16px;
  }
  
  .service-card p {
    font-size: 0.9rem;
    color: var(--ink-secondary);
  }

  .bg-pattern {
    position: absolute;
    top: 20%;
    right: -100px;
    width: 600px;
    height: 600px;
    opacity: 0.04;
    pointer-events: none;
    z-index: 0;
  }
  
  [data-theme="dark"] .bg-pattern { opacity: 0.1; }



  .calligraphy-container {
    position: absolute;
    right: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 400px;
    height: 400px;
    pointer-events: none;
    opacity: 0.15;
    z-index: 0;
  }

  [data-theme="dark"] .calligraphy-container {
    opacity: 0.08;
  }

  .stroke {
    fill: none;
    stroke: var(--ink-primary);
    stroke-width: 2;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-dasharray: 1000;
    stroke-dashoffset: 1000;
    transition: stroke-dashoffset 2.5s cubic-bezier(0.42, 0, 0.58, 1);
  }

  .is-drawn .stroke {
    stroke-dashoffset: 0;
  }

  @keyframes reveal {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }

  .animate-up {
    animation: reveal 0.8s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
    opacity: 0;
  }
  
  .delay-1 { animation-delay: 0.1s; }
  .delay-2 { animation-delay: 0.2s; }
  .delay-3 { animation-delay: 0.3s; }

  @media (max-width: 900px) {
    .paper-surface { grid-template-columns: 40px 1fr; }
    .margin-rail::after { right: 6px; width: 3px; border-width: 1px; }
    .main-content { padding: 40px; }
    .grid-col-4, .grid-col-6 { grid-column: span 12; }
    .hero h1 { font-size: 3.5rem; }
    .bg-pattern { width: 300px; right: -50px; }
    .calligraphy-container { width: 200px; height: 200px; }
  }
</style>

